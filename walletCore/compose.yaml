services:
  postgres:
    image: 'postgres:16-alpine'
    container_name: walletCore_db
    environment:
      - 'POSTGRES_DB=wallet_core'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432:5432'
    volumes:
      - ./postgres-data:/var/lib/postgresql/data